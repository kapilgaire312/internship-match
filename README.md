Folder structure:
 @/actions -> server actions
@/lib/models -> database models
@lib/utils -> utils that can only be called (or used) from server
@/utils -> utils that are called from client (and server)
@/components -> reusable react components
-> components only required in specific routes are in that folder.
